ANGLE_DEG=90 ./.venv/bin/python -c $'import json, math, os, time\nimport zenoh\nangle_deg = float(os.environ["ANGLE_DEG"])\npayload = json.dumps({"timestamp_ns": time.time_ns(), "servo_angle_rad": math.radians(angle_deg)})\nwith zenoh.open(zenoh.Config()) as session:\n    publisher = session.declare_publisher("command")\n    publisher.put(payload)\n    time.sleep(0.1)\nprint(payload)'
